source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in dude.gemspec
gemspec

gem "dry-cli", git: "https://github.com/dry-rb/dry-cli"
